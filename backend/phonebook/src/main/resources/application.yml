spring:
  data:
    mongodb:
      uri: "mongodb://localhost:27017/phonebook"
      auto-index-creation: true

server:
  port: 8081

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG

#debug: true