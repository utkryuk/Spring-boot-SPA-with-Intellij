spring:
  data:
    mongodb:
      database: phonebook
      host: mongodb
      port: 27017
      auto-index-creation: true

server:
  port: 8081

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG

#debug: true